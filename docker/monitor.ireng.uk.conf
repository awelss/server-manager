server {
    listen 80;
    server_name monitor.ireng.uk;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name monitor.ireng.uk;

    ssl_certificate /etc/letsencrypt/live/monitor.ireng.uk/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/monitor.ireng.uk/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Laravel app
    location / {
        proxy_pass http://127.0.0.1:8683;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Reverb WebSocket
    location /app {
        proxy_pass http://127.0.0.1:8684;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
